import React from 'react';
import Formulario from './Formulario';

const Contacto = () => (

    <>
    {
        //estudiantes.map ( c => <Estudiante nombre = { c.nombre } edad = { c.edad } genero = { c.genero } url = {c.url} /> )
    }
    <Formulario />
    </>

)

export default Contacto;